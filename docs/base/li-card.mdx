---
title: li-card元组件
description: 在Tokens基础上的元组件是组成其他业务组件的基础组件
---

# li-card 元组件

### 示例

### 普通卡片（阴影效果）

import Basic from '@site/static/usage/card/basic/index.mdx';

<Basic />

### 带有链接属性的卡片

import WithLink from '@site/static/usage/card/withLink/index.mdx';

<WithLink />

### 带有图片的卡片

import WithImg from '@site/static/usage/card/withImg/index.mdx';

<WithImg />

### 组件属性

| 属性     | 特性     | 描述 | 类型                  | 默认值      |
| -------- | -------- | ---- | --------------------- | ----------- |
| `label`  | `label`  |      | `string \| undefined` | `''`        |
| `rel`    | `rel`    |      | `string \| undefined` | `''`        |
| `styles` | `styles` |      | `string \| undefined` | `undefined` |
| `target` | `target` |      | `string \| undefined` | `''`        |
| `to`     | `to`     |      | `string \| undefined` | `''`        |

### 组件事件

| 事件名称 | 描述 | 类型 |
| -------- | ---- | ---- |
|          |      |      |
|          |      |      |

### 插槽

| 插槽名称 | 描述 |
| -------- | ---- |
|          |      |
|          |      |

### 使用场景

本节为设计人员和开发人员提供了关于在不同上下文中使用此组件的指导原则

import Callout from '@components/global/Callout';

<Callout emoji='❤️'>
  建议使用场景
  <br />
  当在一个页面上显示多个部分时，li-accordion 通常用于减少滚动的需要
</Callout>

<Callout
  type='error'
  emoji='️🚫'>
  避免使用场景
  <br />
  不适合在导航环境下使用
</Callout>

### 可访问性说明

该组件已经通过 LDS 的可访问性规则验证，符合 WCAG 2.1 AA 可访问性指南

<Callout
  type='warning'
  emoji='✅'>
  该组件已经通过LDS的可访问性规则验证，符合WCAG 2.1 AA可访问性指南
</Callout>

<Callout
  type='warning'
  emoji='❎'>
  该组件还未通过LDS的可访问性规则验证，需要进一步定制开发，才能符合WCAG 2.1
  AA可访问性指南
</Callout>
